仕様駆動の開発ループ。実装前に計画とテストケースを提示して承認を得るゲートを必ず挟み、承認後に実装＋テスト作成、テストが緑になるまで反復、3人のレビュアーによる別視点レビュー、最後に実装・テスト・結果を報告する。ユーザーが「ループコーディング」やこの進め方で開発したいと言ったときに使う。

# Loop Coding（仕様駆動の開発ループ）

仕様を、レビュー済み・テスト済みのコードに変えるための、人間参加型の規律あるワークフロー。フェーズを順番に実行する。**承認ゲート（フェーズ3）を飛ばさないこと。**

## フェーズの宣言（必須）
各フェーズに入るときは、その回答の冒頭で現在のフェーズを次の短い表記で1行で宣言する。
- フェーズ1: 仕様読み取り
- フェーズ2: 実装＆テストケース計画
- フェーズ3: 人間チェック
- フェーズ4: 実装＋テスト作成
- フェーズ5: テスト実行
- フェーズ6: 別視点レビュー
- フェーズ7: 報告

## ゲートでの振る舞い（フェーズ3・フェーズ6の停止中に共通）
ゲートは「止まって待つ」場所ではなく、**ユーザーが明示的に OK を出すまで、対話しながら考え続ける場所**である。ゲート中は次のように振る舞う。
- **承認の判定は厳格に**: 「OK」「承認」「進めて」「それでいこう」など、明示的に次へ進む意思が示された場合のみゲートを通過する。質問・感想・「なるほど」「ふむ」・部分的な同意（「ここは良いね」）は承認ではない。迷ったら「この内容で承認してよいですか？」と確認し、勝手に進まない。
- **質問には答え続ける**: ユーザーからの質問（設計の理由、代替案、テストの意図、用語の意味、リスクなど）には、そのゲートに留まったまま丁寧に答える。答えた後は、必ず「現在ゲート中。承認するか、さらに修正・質問があるか」を短く添えて、ゲートに留まっていることを明示する。
- **指摘があれば考え直して再提示する**: 修正要望・懸念・違和感が示されたら、実装計画やテストケースを考え直し、変更点を明示（「変更前 → 変更後」と理由）した上で全体を再提示する。ユーザーが変更点だけ見て判断できるよう、変わっていない部分は簡潔にまとめてよい。
- **自分からも見直す**: ユーザーとのやり取りで新たに分かった事実（仕様の意図、既存コードの制約、運用条件など）があれば、指摘されていなくても計画・テストケースへの影響を自分で確認し、影響があれば修正案として提示する。
- **何度でも繰り返す**: 質問→回答、指摘→再提示のサイクルは、承認が出るまで回数の上限なく繰り返す。「もう十分議論した」と自分で判断して先に進むことはしない。
- **ゲート中はコードを書かない**: ゲート中の再検討で本番コードやテストコードを書き始めない（説明のための短い擬似コード・型・シグネチャの例示は可）。
- **各回答の末尾で状態を示す**: ゲート中の回答は、末尾で「ゲート待機中: 承認 / 修正 / 質問 のいずれかをお願いします」の趣旨を1行で示し、承認待ちであることが常に分かるようにする。

## フェーズ1 — 仕様を受け取る
ユーザーが渡した仕様を読む。スコープ・入出力・境界条件・非対象（やらないこと）に重要な曖昧さがあれば、計画に入る前に簡潔に質問する。仕様を1段落で言い換えて提示し、認識のズレを早期に潰す。

## フェーズ2 — 計画＋テストケースを作る（実装はまだしない）
次の2つ「だけ」を作る。コードは書かない。
1. 実装計画: 追加・変更するファイル/クラス/メソッド、方式、主要な設計判断とトレードオフ、対象外。リポジトリの実構造に合わせて具体的に（実在するファイル名・シンボル名で）書く。
2. テストケース一覧: 書く予定の各テストについて、名前・シナリオ・入力・期待結果。正常系、境界値、異常系（エラー処理）、並行性/順序性の懸念を網羅する。ここではコードではなく説明として書く。
まずコードベースを調べ、計画が実構造に適合していることを確認する。このフェーズでは本番コードもテストコードも書かない。

## フェーズ3 — レビューゲート（停止）
まず、**フェーズ2で作った実装計画とテストケースを、プログラミング学習者にも分かるように解説する**。専門用語はかみ砕き、「何をやるか」だけでなく「なぜその設計・なぜそのテストにするのか」の理由を添える。
その上で、計画とテストケースをユーザーに提示し、**明示的に停止してレビューを求める**。承認があるまで実装に進まない。

このゲートでは「ゲートでの振る舞い」に従う。すなわち、ユーザーが明示的に承認するまで、
- ユーザーの質問に答え続け、
- 指摘・懸念があるたびに実装計画とテストケースを考え直して再提示し、
- 自分でも計画・テストケースの穴（漏れた境界値、異常系、既存コードとの不整合など）を探して提案し、
- 毎回の回答末尾で承認待ちであることを明示する。

再提示のたびに「フェーズ3: 人間チェック」の宣言は維持する（フェーズ2に戻ったと見せない。実質的な作り直しが必要な場合のみ、その旨を告げてフェーズ2に戻る）。このゲートは必須。

## フェーズ4 — 実装＋テスト作成
承認後のみ実行する。
- 環境にバージョン管理が無い、または変更が非自明な場合は、編集前にファイルをバックアップする（環境注記参照）。
- 承認された計画を、最小限で焦点を絞った差分で実装する。
- 承認されたテストケースをそのまま実装する。追加・変更する場合はユーザーに分かるよう明記する。
- 本番コードとテストを計画と一致させる。

## フェーズ5 — テスト実行、緑になるまで反復
テストスイートを実行する。**この環境で実行できない場合（例: ビルドルートが接続フォルダ外、または build ツールが未インストール）は、正確な実行コマンドをユーザーに渡し、結果の貼り付けを待つ。** 失敗時は、原因を切り分け、実装が誤りかテストケースが誤りかを判断し、修正して再実行する。全テストが通るまで繰り返す。各反復を簡潔に報告する。

## フェーズ6 — 別視点レビュー（3人のレビュアーによる反復レビュー）
完成した変更（本番コード・テストコード・仕様との対応・学習者向け解説の下書き）を、次の **3人のレビュアー** に初見のつもりでレビューさせる。独立性を高めるため、可能なら **レビュアーごとに別のサブエージェント** を起動し、それぞれの人物像・関心事・口調を指示して、仕様と差分（必要なら周辺コード）を渡す。サブエージェントが使えない場合は、自分が1人ずつ完全に役になりきって、レビュアーごとに独立した所見を書く（3人分をまとめて薄く書かない）。

### レビュアー
1. **参画したてのエンジニア**（このプロジェクトに入ったばかり。プログラミングは一通りできるが、このリポジトリの慣習・既存コード・ドメインは知らない）
   - 観点: 「このコードは初めて読む人に理解できるか」「命名・構造・コメントは既存の流儀と合っているか」「暗黙の前提（環境、設定、呼び出し順序）が説明されているか」「どこをどう触れば壊れるか分かるか」。分からない箇所は遠慮なく質問として挙げる。
2. **まじで何も知らない中学生**（プログラミングもこの業務も知らない。用語も知らない）
   - 観点: 「これは何のために何をするものなのか、説明を読んで理解できるか」「言葉が難しすぎないか」「例え話や具体例で腑に落ちるか」「『なんでそうするの？』に答えられているか」。コードそのものではなく、フェーズ3・フェーズ7で示す学習者向け解説と、コードの意図説明（コメント・命名）が対象。素朴な疑問をそのまま挙げる。
3. **テックリード級のベテランエンジニア**（設計・運用・障害対応の経験が豊富）
   - 観点: 仕様適合、見落とした境界条件、エラー処理、スレッド安全/並行性、セキュリティ、性能、より単純な代替、保守性、テストの網羅性と妥当性（テストが本当に仕様を検証しているか、壊れやすいテストになっていないか）。

### レビューの進め方（疑問点・指摘がなくなるまで繰り返す）
ラウンドを回す。各ラウンドで3人全員にレビューさせ、所見を **レビュアー別・番号付き** で集め、次の3種に分類して対応する。
1. **不具合（バグ・仕様違反）**: その場で修正し、テストを再実行して緑を確認する（フェーズ5と同じ規律）。
2. **疑問・分かりにくさ（質問、理解できない点、説明不足）**: 原因がコード側にあるなら、命名・コメント・構造・エラーメッセージの改善で解消する。原因が説明側にあるなら、学習者向け解説の内容（用語のかみ砕き、具体例、「なぜ」の説明）を改善して解消する。いずれの場合も、そのレビュアーに向けた回答を書く。
3. **改善提案（不具合ではないがより良くできる点）**: その場では修正せず、ユーザー向けに保留する。「現状 → こう改善できる（理由・効果、おおよその手間とリスク）」の形で整理しておく。

対応後、次のラウンドとして **修正後のコード・解説と、前ラウンドの各所見への対応内容** を3人に再度渡し、「前回の指摘は解消されたか」「新たな疑問・指摘はないか」を確認させる。**3人全員が「疑問点・指摘なし（保留中の改善提案を除く）」と答えるまでラウンドを繰り返す。** 自分の判断で「もう十分」と打ち切らない。ただし、5ラウンド回しても収束しない場合は、残っている所見と収束しない理由を整理してユーザーに提示し、判断を仰ぐ（これもゲート扱い）。

各ラウンドの結果は、フェーズ7で報告できるよう、レビュアー別に「所見 → 分類 → 対応（何をどう変えたか、または回答）→ 次ラウンドでの確認結果」を記録しておく。

### 改善提案の扱い（ゲート）
レビューが収束したら、保留していた改善提案をまとめてユーザーに提示し、一旦停止して判断を仰ぐ。この停止もゲートであり、「ゲートでの振る舞い」に従う（提案についての質問に答え続け、指摘があれば提案内容や評価を考え直して再提示し、ユーザーが各提案の採否を明示するまで先に進まない）。
- ユーザーが「必要」と判断した提案は、新たな改善として **フェーズ1（仕様の受け取り）または フェーズ2（実装計画・テストケース作成）に戻ってループを再度回す**。
- ユーザーが「不要」または「今回は見送り」とした提案は、理由とともに記録してフェーズ7へ進む。
改善提案が無い場合は、その旨を述べてフェーズ7へ進む。

## フェーズ7 — 報告
次をまとめて報告する: 実装内容（ファイル/シンボル）、テストケースと網羅範囲、テスト結果（成否・件数・要した反復回数）、別視点レビューの所見（修正した不具合と、提示した改善提案の採否）、既知の制限とフォローアップ。

### レビュー対応の明示（必須）
フェーズ6の記録をもとに、**レビュアーごと** に、出た指摘・疑問と、それにどう対応したかを明示する。形式は次のとおり（表または箇条書き）。
- レビュアー名（参画したてのエンジニア / 中学生 / テックリード）
  - ラウンドN: 所見の内容 → 分類（不具合 / 疑問・分かりにくさ / 改善提案）→ 対応（具体的に何をどう変えたか、または回答内容。改善提案はユーザーの採否と理由）→ 確認結果（次ラウンドで解消と判定されたか）
- 最終的に全員が「指摘なし」と答えたラウンド番号と、要した総ラウンド数。
- 5ラウンドで収束せずユーザー判断に委ねた場合は、その残件と判断結果。
指摘が1件もなかったレビュアーがいる場合も「指摘なし」と明記する（省略しない）。

### 学習者向け解説（必須・従来どおり）
さらに、プログラミング学習者向けの解説を含める。解説では、**実際に変更・追加したコードをコードブロックで提示してから説明する**（どんなコードにしたかをユーザーが目で確認できるようにするため）。
- **フェーズ4で書いた本番コードの解説**: 実際に変更・追加した本番コード（該当箇所のスニペットまたは diff）を提示し、その上で主要な変更が何をしているか・なぜそう書くのかをかみ砕いて説明する。フェーズ6のレビュー対応で変わった箇所は、最終版を提示する。
- **フェーズ4で書いたテストコードの解説**: 実際に変更・追加したテストコード（該当箇所のスニペットまたは diff）を提示し、各テストが何を確認しているかをかみ砕いて説明する。
- **フェーズ6のレビュー内容の解説**: 3人がそれぞれどんな観点で見たか、何を直し・何を見送ったかを、かみ砕いて説明する（上の「レビュー対応の明示」と重複してよいが、こちらは学習者が読んで分かる言葉で書く）。
コード提示は、変更・追加した部分を中心にする（ファイル全体が大きい場合は、変更点の周辺に絞って提示する）。

## 環境注記
- ビルド/テストが現在の環境で実行できない場合（例: pom.xml など build ルートが接続フォルダ外、または実行環境に build ツールが無い）は、フェーズ5は人間が実行する。正確なコマンドを渡して結果を待つ。私が直接回したい場合は、build ルートごと接続して開始してもらう。
- 作業領域にバージョン管理が無い場合は、非自明な編集の前にバックアップを取る（例: 近傍の `_to_delete/` などへ退避）。
- 差分は最小限にし、各変更は可能な範囲で（構文/コンパイル確認など）検証してから次へ進む。
- 各フェーズ開始時に、上記「フェーズの宣言」の表記で現在フェーズを1行宣言する。
- フェーズ6のレビュアーは、可能な限りレビュアーごとに独立したサブエージェントで動かす。サブエージェントが使えない場合は、自分が1人ずつ役になりきって独立した所見を書く。