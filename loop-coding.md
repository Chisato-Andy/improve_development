仕様駆動の開発ループ。実装前に計画とテストケースを提示して承認を得るゲートを必ず挟み、承認後に実装＋テスト作成、テストが緑になるまで反復、別視点レビュー、最後に実装・テスト・結果を報告する。ユーザーが「ループコーディング」やこの進め方で開発したいと言ったときに使う。

# Loop Coding（仕様駆動の開発ループ）

仕様を、レビュー済み・テスト済みのコードに変えるための、人間参加型の規律あるワークフロー。フェーズを順番に実行する。**承認ゲート（フェーズ3）を飛ばさないこと。**

## フェーズ1 — 仕様を受け取る
ユーザーが渡した仕様を読む。スコープ・入出力・境界条件・非対象（やらないこと）に重要な曖昧さがあれば、計画に入る前に簡潔に質問する。仕様を1段落で言い換えて提示し、認識のズレを早期に潰す。

## フェーズ2 — 計画＋テストケースを作る（実装はまだしない）
次の2つ「だけ」を作る。コードは書かない。
1. 実装計画: 追加・変更するファイル/クラス/メソッド、方式、主要な設計判断とトレードオフ、対象外。リポジトリの実構造に合わせて具体的に（実在するファイル名・シンボル名で）書く。
2. テストケース一覧: 書く予定の各テストについて、名前・シナリオ・入力・期待結果。正常系、境界値、異常系（エラー処理）、並行性/順序性の懸念を網羅する。ここではコードではなく説明として書く。
まずコードベースを調べ、計画が実構造に適合していることを確認する。このフェーズでは本番コードもテストコードも書かない。

## フェーズ3 — レビューゲート（停止）
計画とテストケースをユーザーに提示し、**明示的に停止してレビューを求める**。承認があるまで実装に進まない。修正要望があれば反映して再提示する。このゲートは必須。

## フェーズ4 — 実装＋テスト作成
承認後のみ実行する。
- 環境にバージョン管理が無い、または変更が非自明な場合は、編集前にファイルをバックアップする（環境注記参照）。
- 承認された計画を、最小限で焦点を絞った差分で実装する。
- 承認されたテストケースをそのまま実装する。追加・変更する場合はユーザーに分かるよう明記する。
- 本番コードとテストを計画と一致させる。

## フェーズ5 — テスト実行、緑になるまで反復
テストスイートを実行する。**この環境で実行できない場合（例: ビルドルートが接続フォルダ外）は、正確な実行コマンドをユーザーに渡し、結果の貼り付けを待つ。** 失敗時は、原因を切り分け、実装が誤りかテストケースが誤りかを判断し、修正して再実行する。全テストが通るまで繰り返す。各反復を簡潔に報告する。

## フェーズ6 — 別視点レビュー（初見での独立レビュー）
完成した変更を初めて見るつもりでレビューする。独立性を高めるため、可能ならサブエージェントにレビューさせる。確認観点: 仕様適合、見落とした境界条件、エラー処理、スレッド安全/並行性、セキュリティ、性能、より単純な代替。実在の問題は修正し、見送るものは理由を添えて記録する。

## フェーズ7 — 報告
次をまとめて報告する: 実装内容（ファイル/シンボル）、テストケースと網羅範囲、テスト結果（成否・件数・要した反復回数）、別視点レビューの所見、既知の制限とフォローアップ。

## 環境注記
- ビルド/テストが現在の環境で実行できない場合（例: pom.xml など build ルートが接続フォルダ外）は、フェーズ5は人間が実行する。正確なコマンドを渡して結果を待つ。私が直接回したい場合は、build ルートごと接続して開始してもらう。
- 作業領域にバージョン管理が無い場合は、非自明な編集の前にバックアップを取る（例: 近傍の `_to_delete/` などへ退避）。
- 差分は最小限にし、各変更は可能な範囲で（構文/コンパイル確認など）検証してから次へ進む。
- 各フェーズの区切りで、今どのフェーズにいるかを一言添える。